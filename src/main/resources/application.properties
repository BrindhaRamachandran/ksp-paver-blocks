# Application
spring.application.name=demo
logging.level.root=INFO
server.port=8080

# ----- MySQL datasource -----
# Use host.docker.internal so the container can reach your PC's MySQL
spring.datasource.url=jdbc:mysql://host.docker.internal:3306/KSP_Pavers
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ----- Email (Gmail example) -----
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=rbrindha2003@gmail.com
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ----- H2 (Optional fallback, leave here if needed) -----
# spring.datasource.url=jdbc:h2:mem:demo_db
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.h2.console.enabled=true
