# Render Requirements
server.port=${PORT:10000}
server.address=0.0.0.0

# Render PostgreSQL
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/ksp_db}
spring.datasource.username=${DB_USERNAME:Brindha}
spring.datasource.password=${DB_PASSWORD:Brindha@31}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false

# Email
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=info@ksp-pavers.co.in
spring.mail.password=${MAIL_PASSWORD:gnumgfhdyqembemm}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
